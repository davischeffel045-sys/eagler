# Copie este arquivo para .env se for rodar localmente (não é necessário no Render,
# lá as variáveis são cadastradas na aba "Environment" do serviço).

# Host do seu servidor Aternos (sem porta)
TARGET_HOST=Davi_Gabriel__.aternos.me

# Porta do seu servidor Aternos
TARGET_PORT=15630

# Porta em que o proxy escuta (o Render define isso sozinho via PORT)
PORT=8080
